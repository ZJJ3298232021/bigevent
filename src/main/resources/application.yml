# 通用配置
spring:
  main:
    banner-mode: off
  profiles:
    active: pro
mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    banner: off
server:
  servlet:
    context-path: /